<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Issue Table</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    html {
      scroll-behavior: smooth;
      font-family: 'Segoe UI', 'Roboto', sans-serif;
      font-size: 16px;
    }

    body {
      min-height: 100vh;
      background: linear-gradient(135deg, #0f2027, #203a43, #2c5364);
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: 30px 20px;
      overflow-x: hidden;
    }

    @keyframes fadeInUp {
      from { opacity: 0; transform: translateY(30px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .message {
      background: rgba(255, 255, 255, 0.1);
      padding: 16px 24px;
      border-radius: 14px;
      margin-bottom: 30px;
      font-size: 18px;
      color: #fff;
      text-align: center;
      box-shadow: 0 8px 24px rgba(0, 0, 0, 0.3);
      animation: fadeInUp 0.4s ease;
    }

    table {
      width: 100%;
      max-width: 1050px;
      background: rgba(255, 255, 255, 0.08);
      border-collapse: collapse;
      border-radius: 20px;
      overflow: hidden;
      backdrop-filter: blur(12px);
      -webkit-backdrop-filter: blur(12px);
      box-shadow: 0 12px 28px rgba(0,0,0,0.35);
      animation: fadeInUp 0.5s ease;
    }

    th, td {
      padding: 16px 20px;
      text-align: left;
      color: #fff;
      border-bottom: 1px solid rgba(255,255,255,0.1);
      font-size: 0.95rem;
    }

    th {
      background-color: rgba(255,255,255,0.12);
      font-weight: 600;
      font-size: 1rem;
      letter-spacing: 0.5px;
    }

    tr:hover {
      background-color: rgba(255,255,255,0.04);
    }

    .btn {
      margin-top: 24px;
      padding: 12px 22px;
      font-size: 1rem;
      font-weight: bold;
      color: #fff;
      background: linear-gradient(135deg, #00d2ff, #3a47d5);
      border: none;
      border-radius: 12px;
      cursor: pointer;
      transition: transform 0.2s ease, background 0.4s;
    }

    .btn:hover {
      transform: scale(1.03);
      background: linear-gradient(135deg, #0072ff, #00c6ff);
    }

    .nav-btn {
      position: absolute;
      top: 20px;
      right: 20px;
    }

    .issue-btn {
      position: absolute;
      top: 80px;
      right: 20px;
      background: linear-gradient(135deg, #1de9b6, #1dc4e9);
    }

    .back-btn {
      margin-top: 35px;
    }

    @media (max-width: 768px) {
      table, thead, tbody, th, td, tr {
        display: block;
      }

      thead {
        display: none;
      }

      tr {
        margin-bottom: 20px;
        background-color: rgba(255, 255, 255, 0.08);
        border-radius: 12px;
        padding: 14px;
      }

      td {
        text-align: right;
        padding-left: 50%;
        position: relative;
        border-bottom: none;
      }

      td::before {
        content: attr(data-label);
        position: absolute;
        left: 16px;
        top: 16px;
        font-weight: bold;
        color: #ffffffcc;
        text-align: left;
      }
    }


    .filter-form {
      display: flex;
      align-items: center;
      justify-content: flex-start;
      gap: 10px;
      margin-bottom: 20px;
      width: 100%;
      max-width: 1050px;
    }

    .filter-select, .filter-btn {
      padding: 10px 16px;
      border-radius: 8px;
      font-size: 0.95rem;
      border: none;
      outline: none;
    }

    .filter-select {
      background-color: #ffffff15;
      color: #fff;
    }

    .filter-btn {
      background: #3a47d5;
      color: #fff;
      cursor: pointer;
      font-weight: bold;
    }

    .filter-btn:hover {
      background: #5b6ef7;
    }
  </style>
</head>
<body>

  <button class="btn nav-btn" onclick="window.location.href='/logout'">Logout</button>
  <button class="btn issue-btn" onclick="window.location.href='/issue_form'">+ Raise New Issue</button>

  <div class="message">All Issues Submitted by You</div>

  <!-- 🔍 Filter Form -->
  <form method="GET" action="/issue_list" class="filter-form">
    <select name="status" class="filter-select">
      <option value="">All Status</option>
      <option value="pending" {{#ifCond filterStatus '==' 'pending'}}selected{{/ifCond}}>Pending</option>
      <option value="resolved" {{#ifCond filterStatus '==' 'resolved'}}selected{{/ifCond}}>Resolved</option>
    </select>

    <select name="sort" class="filter-select">
      <option value="newest" {{#ifCond filterSort '==' 'newest'}}selected{{/ifCond}}>Newest First</option>
      <option value="oldest" {{#ifCond filterSort '==' 'oldest'}}selected{{/ifCond}}>Oldest First</option>
    </select>

    <button type="submit" class="filter-btn">Go</button>
  </form>

  <table>
    <thead>
      <tr>
        <th>Name</th>
        <th>Email</th>
        <th>Department</th>
        <th>Subject</th> 
        <th>Description</th>
        <th>Status</th>
      </tr>
    </thead>
    <tbody>
      {{#each users}}
      <tr>
        <td data-label="Name">{{this.username}}</td>
        <td data-label="Email">{{this.email}}</td>
        <td data-label="Department">{{this.department}}</td>
        <td data-label="Subject">{{this.subject}}</td>
        <td data-label="Description">{{this.description}}</td>
        <td data-label="Status">{{this.status}}</td>
      </tr>
      {{/each}}
    </tbody>
  </table>

  <script>
    const params = new URLSearchParams(window.location.search);
    if (params.get("login") === "success") {
      const toast = document.createElement("div");
      toast.className = "toast";
      toast.innerText = "✅ Successfully Logged In!";
      document.body.appendChild(toast);
      setTimeout(() => toast.remove(), 3000);
    }
  </script>

  <style>
    .toast {
      position: fixed;
      bottom: 30px;
      left: 50%;
      transform: translateX(-50%);
      background-color: #28a745;
      color: #fff;
      padding: 14px 24px;
      border-radius: 8px;
      font-weight: bold;
      z-index: 9999;
      box-shadow: 0 6px 16px rgba(0,0,0,0.3);
    }
  </style>
</body>
</html>